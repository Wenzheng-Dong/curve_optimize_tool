# ---------------------------------------------------------------
# 版本控制约定（2026-08-09 修订，取代 2026-07-28 的旧约定）：
#   本仓库有公开 GitHub remote。计划 / 协作 / 开发日志 / 未发表手稿
#   一律【不进 git】——它们留在本地磁盘，不靠 "push 被 deny" 当护栏。
#   旧约定曾把它们纳入本地追踪并写 "将来放 GitHub 前再决定哪些内容剥离"，
#   这个剥离步骤在建 remote 时被跳过，导致内容公开外泄；已用
#   git filter-repo 从全历史清除。同样的规则也写进 .git/info/exclude，
#   跨分支生效，防止 checkout 到旧分支后被重新 add。
#   合并到 main 必须经用户确认。
# ---------------------------------------------------------------

# 绝不上传：内部规划 / 协作纪律 / 开发日志 / 未发表手稿
/_plan.md
/_plan_full_cost.md
/_thoughts.md
/_dev_logs/
/_self_study/
/AGENTS.md
/CLAUDE.md

# 优化运行的原始数据（系数轨迹 / checkpoint，可能很大）：不追踪。
# 数据本身是可追溯资产，保存在本地磁盘，由 _runs/ 内 manifest 自述。
/_runs/

# macOS
.DS_Store

# Claude Code 整个目录：agent 定义（builder.md / leader.md）成段复述了协作纪律
# 与计划文档的章节结构，跟上面那组同类；settings.local.json 还含机器相关路径。
/.claude/

# Python 构建 / 缓存产物
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# 环境
.venv/
venv/

# stray scratch dir, not part of this repo
proposals/

# LaTeX build artifacts (manuscripts under _self_study/_write-up/)
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz

# F08 golden-regression snapshots: ~5 MB of .npz regenerated by
# `python _dev_logs/F08_golden.py before|after`. Working data, like _runs/ --
# the script and the compared counts are what get committed, not the blobs.
/_dev_logs/F08_golden/
